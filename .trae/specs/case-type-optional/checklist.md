# 测试用例类型可选功能 - 验证清单

## 前端验证
- [x] 用例类型表单项没有 required 属性（红色星号）
- [x] 页面初始化时，用例类型选择框显示占位文字，没有预选项
- [x] 点击重置按钮后，用例类型选择框恢复到占位文字状态
- [x] 占位文字清晰提示"不选择则由AI智能判断"

## 后端验证
- [x] AIGenerateEnhancedRequest 模型的 case_type 字段为 Optional[str] 类型
- [x] AIGenerateEnhancedRequest 模型的 case_type 没有默认值
- [x] 发送不带 case_type 的请求，后端正常处理，不返回 422 验证错误
- [x] 发送 case_type 为空字符串的请求，后端正常处理

## 功能验证
- [ ] 不选用例类型时，生成的测试用例类型由 AI 智能判断（可能包含多种类型）
- [ ] 选择"UI自动化"时，所有生成的用例类型都是 ui_automation
- [ ] 选择"API自动化"时，所有生成的用例类型都是 api_automation
- [ ] 选择"手工测试"时，所有生成的用例类型都是 manual
- [ ] 保存 AI 生成的用例时，用例类型保持与生成结果一致，不被默认值覆盖

## 兼容性验证
- [ ] 选用例类型生成的功能保持正常工作
- [ ] 生成的用例能正常保存到数据库
- [ ] 生成的用例能正常在列表中显示
- [ ] 生成的用例类型标签显示正确
