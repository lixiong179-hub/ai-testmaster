# AI TestMaster 技术架构文档（智能体上下文版）

## 1. 项目概述

### 1.1 背景与愿景
AI TestMaster 是一个面向企业的 AI 全自动测试平台，致力于通过 AI 技术简化软件测试全流程。用户只需上传需求文档，平台即可自动分析、生成测试用例、执行测试并输出详细报告，大幅提升测试效率与质量。

### 1.2 核心价值
- **全流程自动化**：从需求到报告完全自动化，减少人工干预。
- **AI 驱动**：利用大语言模型（DeepSeek）生成高质量测试用例和智能失败分析。
- **多项目隔离**：支持多个项目独立运行，数据、权限、文件存储完全隔离，满足企业级安全要求。
- **高扩展性**：模块化设计，可灵活接入新测试框架、AI 模型或执行环境。

### 1.3 关键挑战
- 多项目数据隔离与安全性保障。
- AI 生成用例的准确性与成本控制。
- 大规模测试任务的并发执行与资源调度。
- 系统的高可用与可观测性。

## 2. 核心约束与设计原则

### 2.1 多项目完全隔离
- **数据隔离**：所有业务数据通过 `project_id` 逻辑隔离，查询强制携带项目上下文。
- **文件存储隔离**：MinIO 中为每个项目创建独立 Bucket 或使用项目前缀（如 `project/{project_id}/`），设置桶策略禁止跨项目访问。
- **权限隔离**：RBAC 模型扩展为项目级权限，角色与权限绑定项目，权限校验同时验证用户-角色-项目关系。
- **资源隔离**：自动化测试执行使用 Kubernetes Namespace 或独立 Pod 进行资源隔离，避免相互干扰。

### 2.2 高可用与扩展性
- **无状态服务**：应用层服务均设计为无状态，支持水平扩展。
- **数据库主从**：MySQL 主从复制，读写分离，提升读性能与容错。
- **缓存集群**：Redis 哨兵或集群模式，保证缓存高可用。
- **消息队列**：引入 RabbitMQ/Kafka 解耦任务调度与执行，支持削峰填谷与失败重试。

### 2.3 安全合规
- **传输加密**：全链路 HTTPS，内部服务间可启用 mTLS。
- **数据脱敏**：敏感字段加密存储，日志输出自动过滤敏感信息。
- **审计日志**：关键操作记录至独立审计库，保证不可篡改。
- **API 安全**：JWT/OAuth2 认证，细粒度权限控制，请求限流。

## 3. 总体架构

### 3.1 分层架构图（文本描述）

- **用户层**：用户（测试人员）通过 Web 前端访问，第三方系统通过 API 调用。
- **接入层**：Nginx 负载均衡，API 网关（Kong/Nginx+Lua）进行路由、认证、限流。
- **应用层**：多个微服务（测试用例管理、任务调度、报告生成、用户管理、权限管理）。
- **AI 层**：DeepSeek API 客户端、提示词引擎、测试用例生成器、失败分析器。
- **自动化层**：测试执行器（支持 Selenium、Appium、pytest）、Allure 报告集成。
- **数据层**：MySQL（主从）、Redis（缓存）、MinIO（对象存储）、RabbitMQ（消息队列）。
- **基础设施层**：Docker 容器化，Kubernetes 编排，Prometheus+Grafana 监控，EFK 日志收集。

### 3.2 架构视图说明

所有服务均以容器化方式运行于 Kubernetes 集群，通过服务发现相互调用。API 网关统一对外暴露接口，处理认证与路由。应用层服务通过消息队列将任务下发至自动化执行器，执行结果回调更新状态。

## 4. 模块详细设计

### 4.1 接入层
- **Nginx**：负责 SSL 终止、负载均衡、静态资源缓存。
- **API 网关**：基于 Kong 或自研 Nginx+Lua，实现路由转发、JWT 校验、请求限流、黑白名单。
- **认证授权服务**：独立微服务，处理用户登录、JWT 颁发与刷新，集成 OAuth2 供第三方接入。

### 4.2 应用层
- **测试用例管理服务**：CRUD 操作，支持导入导出，关联需求文档，调用 AI 层生成用例。
- **任务调度服务**：接收测试任务，持久化任务记录，发送消息到队列，监听执行结果更新状态。
- **报告生成服务**：解析 Allure 报告，提取关键数据存入数据库，生成可读性强的 HTML/PDF 报告。
- **用户管理服务**：用户注册、登录、个人信息管理。
- **权限管理服务**：基于 RBAC 模型，管理角色、权限、用户-角色-项目关系。

### 4.3 AI 层
- **DeepSeek API 客户端**：封装对 DeepSeek 模型的调用，支持同步/异步请求，处理重试与限流。
- **提示词引擎**：为不同测试类型（Web UI、API、移动端）设计专用提示词模板，支持动态变量替换。
- **测试用例生成器**：流程：需求文档上传 → 文本预处理 → 调用 DeepSeek 生成测试点 → 结构化输出（JSON） → 转换为标准测试用例（如 pytest 脚本）并存储。
- **失败分析器**：流程：捕获执行日志/错误截图 → 结合用例上下文构造提示 → 调用 DeepSeek 分析根因 → 生成建议报告并关联至执行记录。
- **成本控制**：记录每次 AI 调用的 token 使用量（表 `AI_LOG`），设置项目级别的月度配额，超限告警。

### 4.4 自动化层
- **测试执行器**：独立服务，监听消息队列，根据任务类型调用对应执行器（Web、移动、API）。采用适配器模式集成不同测试框架。
- **分布式执行**：利用 Kubernetes Job 为每个测试任务创建独立 Pod 运行，或使用 Celery 工作池，实现并发执行与资源隔离。
- **结果聚合**：执行完成后，解析 Allure 生成的 JSON 报告，提取测试用例结果、错误信息、执行时间等存入数据库，同时将完整 HTML 报告上传至 MinIO。
- **失败重试**：支持任务失败自动重试（可配置重试次数），避免瞬时故障影响。

### 4.5 数据层
- **MySQL 主从**：主库负责写操作，从库负责读操作，使用 ProxySQL 或应用层读写分离。
- **Redis 缓存**：缓存热点数据（用户信息、项目配置、权限信息），减少数据库压力。
- **MinIO 对象存储**：存储需求文档、测试报告、执行日志、截图等文件，按项目隔离。
- **消息队列**：RabbitMQ 用于任务调度，确保消息可靠投递；Kafka 可选用于日志收集和事件驱动。

## 5. 多项目隔离实现

| 隔离维度 | 实现方案 |
|----------|----------|
| **数据隔离** | 所有业务表增加 `project_id` 字段，查询时强制添加该条件；数据库用户权限仅允许访问对应 schema（若采用物理隔离）。 |
| **文件存储隔离** | MinIO 中每个项目分配独立 Bucket 或使用前缀，桶策略限制只能访问自己的文件。 |
| **权限隔离** | 权限模型增加项目维度：`user_project_role` 关联表记录用户在某项目中的角色；权限校验时检查用户是否具有该项目下的相应权限。 |
| **资源隔离** | 测试执行任务在 Kubernetes 中调度到指定 Namespace，并设置资源配额（CPU/内存限制），避免项目间资源抢占。 |

## 6. 数据架构

### 6.1 数据库 ER 图（略，见原始文档）

### 6.2 分库分表策略
- 初期单库，随着数据量增长可按项目 ID 进行水平分库（如 `project_id % 16` 映射到不同库）。
- 大表如 `execution_record` 可按时间分区（按月），便于归档和查询。

### 6.3 缓存设计
- 缓存粒度：用户会话、项目配置、权限树、常用测试用例列表。
- 缓存失效：数据更新时主动删除或设置过期时间。
- 缓存穿透/雪崩保护：布隆过滤器、随机过期时间、本地二级缓存（Caffeine）。

### 6.4 文件存储方案
- 文件路径格式：`project/{project_id}/{type}/{date}/{uuid}.{ext}`，其中 `type` 为 `requirement`、`report`、`log`、`screenshot` 等。
- 文件访问：生成预签名 URL 供前端直接下载，避免后端代理大文件。

## 7. 接口设计

### 7.1 API 风格与规范
- RESTful 风格，资源使用复数名词，路径中携带项目 ID：`/api/v1/projects/{project_id}/testcases`。
- 请求/响应格式统一为 JSON，包含状态码、消息、数据体。
- 版本控制通过 URL 路径（v1, v2）实现。

### 7.2 认证与授权
- **认证**：JWT（Access Token + Refresh Token），Token 中携带用户 ID 和项目列表（可选）。
- **授权**：权限服务提供中间件，解析 Token 后校验用户对指定项目是否具有所需权限（如 `testcase:create`）。
- **第三方系统**：支持 API 密钥认证，密钥绑定项目，调用时需携带 `X-API-Key` 头。

### 7.3 版本管理
- 主版本号变更表示不兼容更新，次版本号表示向下兼容的功能新增，修订号表示 bug 修复。
- 每个版本都有独立文档，支持多版本并行一段时间。

## 8. 部署架构

### 8.1 部署拓扑图（文本描述）

- **Kubernetes 集群**：所有服务运行在 K8s 中，分为多个 Namespace（如 `prod`、`test`、`dev`）。
- **前端**：Vue 应用打包为静态文件，通过 Nginx Ingress 对外服务，CDN 加速静态资源。
- **后端微服务**：每个服务部署多个副本，配置 HPA 基于 CPU/内存自动伸缩。
- **中间件**：MySQL 主从部署于 StatefulSet，Redis 哨兵模式，RabbitMQ 集群，MinIO 分布式模式。
- **监控与日志**：Prometheus Operator 管理监控，Grafana 展示；Fluentd 采集日志到 Elasticsearch，Kibana 查询。

### 8.2 环境划分
- **开发环境**：开发者本地或共享集群 namespace，配置简化。
- **测试环境**：用于集成测试，部署与生产环境尽可能一致。
- **生产环境**：多可用区部署，保证高可用。

### 8.3 容器化与编排
- Dockerfile 多阶段构建，减小镜像体积。
- 使用 Helm 管理应用发布，支持环境差异化配置。
- CI/CD 流水线：Git 触发 → 单元测试 → 构建镜像 → 推送到私有仓库 → 更新 K8s 部署。

## 9. 运维与可观测性

### 9.1 监控体系
- **指标收集**：Prometheus 采集各服务指标（请求数、延迟、错误率、CPU/内存等）。
- **监控大盘**：Grafana 提供全局视图、服务详情视图、项目视图。
- **告警规则**：基于 Prometheus Alertmanager 配置告警（如 5xx 错误率 > 1%、P99 延迟 > 500ms）。

### 9.2 日志管理
- **日志采集**：Fluentd 以 DaemonSet 形式采集容器 stdout 和日志文件，发送至 Elasticsearch。
- **日志查询**：Kibana 提供集中查询界面，支持按服务、项目、时间、关键词过滤。
- **日志告警**：ElastAlert 或 X-Pack Watcher 基于日志内容触发告警。

### 9.3 告警机制
- **告警通道**：接入钉钉/企业微信/邮件，根据级别分发。
- **告警抑制**：防止告警风暴，配置分组与延迟规则。

### 9.4 备份与恢复
- **数据库备份**：每天全量备份，每小时增量备份（binlog），备份文件存储至 MinIO 异地。
- **对象存储备份**：MinIO 本身支持跨站点复制，关键数据可同步至另一区域。
- **恢复演练**：定期进行恢复测试，确保 RPO/RTO 达标。

## 10. 非功能需求

### 10.1 性能指标
- 单接口响应时间 ≤ 200ms（P95）。
- 支持 100 个并发测试任务同时执行。
- 万级用例查询响应时间 ≤ 1s（加索引优化）。
- AI 测试用例生成响应时间 ≤ 5s，失败分析 ≤ 3s。

### 10.2 安全性
- 符合 ISO27001 信息安全管理体系标准。
- 所有传输 HTTPS，敏感数据加密存储。
- 基于 RBAC 的细粒度权限控制，操作审计日志。
- API 限流：每个项目每秒最多 100 次请求，超过返回 429。

### 10.3 可靠性
- 系统可用性 ≥ 99.9%（全年停机时间 ≤ 8.76 小时）。
- 核心服务（API 网关、认证、任务调度）多副本部署，单点故障不影响整体。
- 消息队列持久化，消费者宕机后消息不丢失。
- 数据库主从自动故障转移（使用 Orchestrator 或 MySQL Router）。

### 10.4 可维护性
- 代码规范：Python 遵循 PEP8，前端遵循 ESLint Airbnb。
- 提供完整的 API 文档（Swagger/OpenAPI）、部署文档、用户手册。
- 关键业务逻辑有单元测试覆盖（目标 80%），集成测试覆盖主要流程。

### 10.5 可扩展性
- 新增测试框架：通过实现执行器适配器即可快速接入。
- 新增 AI 模型：抽象模型客户端接口，可替换为其他 LLM。
- 新增项目隔离方式：可切换为物理库或独立 schema 而不影响上层业务。

## 11. 技术栈清单（优化版）

| 分层 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 前端 | Vue | 3.4 | 前端框架 |
| 前端 | Vite | 5.0 | 构建工具 |
| 前端 | Element Plus | 2.8 | UI组件库 |
| 前端 | Pinia | 2.0 | 状态管理 |
| 前端 | Axios | 1.6 | HTTP客户端 |
| 后端 | Python | 3.10 | 编程语言 |
| 后端 | FastAPI | 0.104 | Web框架 |
| 后端 | Uvicorn | 0.24 | ASGI服务器 |
| 后端 | SQLAlchemy | 2.0 | ORM框架 |
| 后端 | Celery | 5.3 | 分布式任务队列（可选） |
| 自动化层 | Selenium | 4.15 | Web自动化测试 |
| 自动化层 | Appium | 2.5 | 移动自动化测试 |
| 自动化层 | pytest | 7.4 | 测试框架 |
| 自动化层 | Allure | 2.24 | 测试报告 |
| AI层 | DeepSeek | deepseek-chat | AI模型 |
| AI层 | LangChain | 0.1 | 提示词工程 |
| 数据层 | MySQL | 8.0 | 关系型数据库 |
| 数据层 | Redis | 7.0 | 缓存 |
| 数据层 | MinIO | RELEASE.2023 | 对象存储 |
| 数据层 | RabbitMQ | 3.12 | 消息队列 |
| 基础设施 | Docker | 25.0 | 容器化 |
| 基础设施 | Kubernetes | 1.28 | 容器编排 |
| 基础设施 | Nginx | 1.25 | 负载均衡/Ingress |
| 基础设施 | Prometheus | 2.45 | 监控 |
| 基础设施 | Grafana | 10.2 | 监控可视化 |
| 基础设施 | Elasticsearch | 8.11 | 日志存储 |
| 基础设施 | Fluentd | 1.16 | 日志采集 |
| 基础设施 | Kibana | 8.11 | 日志查询 |

## 12. 术语表

| 术语 | 解释 |
|------|------|
| 项目 | 测试资产的集合，包含用例、任务、报告等，数据完全隔离的最小单位。 |
| RBAC | 基于角色的访问控制，通过角色分配权限。 |
| 测试执行器 | 负责运行具体测试框架的服务，可分布式部署。 |
| 提示词模板 | 预定义的用于引导 AI 生成特定类型内容的文本模板。 |
| 多租户 | 本架构中的“多项目”可视为轻量级多租户实现。 |
| RPO/RTO | 恢复点目标（数据丢失量）与恢复时间目标（恢复时长）。 |

---
> 文档版本：1.0  
> 最后更新：2025-03-13  
> 维护者：架构组