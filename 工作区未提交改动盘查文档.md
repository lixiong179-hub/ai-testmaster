# 工作区未提交改动盘查文档

盘查时间：2026-05-28  
工作目录：`D:\PythonFile\ai-testmaster`  
当前分支：`main`  
远程状态：`main...origin/main`，无 ahead / behind  
最新提交：`8edb467 fix: stabilize AI JSON parsing and e2e checks`

## 结论

当前远程已包含已确认并推送的提交 `8edb467`。本地工作区仍存在大规模未提交内容，不能直接执行 `git add .`、批量提交或部署。

本次盘查只新增此文档，未回滚、未提交、未推送其它本地改动。

## 总体统计

| 类型 | 数量 / 规模 |
| --- | ---: |
| 已跟踪但未提交文件 | 380 |
| 其中修改文件 | 365 |
| 其中删除文件 | 15 |
| 未跟踪文件 | 72 |
| `git status --porcelain` 总条目 | 467 |
| 已跟踪 diff 规模 | 380 files changed, 22274 insertions(+), 9958 deletions(-) |
| 暂存区 | 空 |

## 按顶层目录统计

### 已跟踪未提交变更

| 顶层路径 | 数量 |
| --- | ---: |
| `src` | 178 |
| `app` | 133 |
| `tests` | 43 |
| `cypress` | 15 |
| 根目录配置/单文件 | 10 |
| `docs` | 1 |

### 未跟踪文件

| 顶层路径 | 数量 |
| --- | ---: |
| `tests` | 20 |
| `src` | 18 |
| `app` | 14 |
| `scripts` | 6 |
| `alembic` | 5 |
| `docs` | 3 |
| `test-results` | 2 |
| 其它根目录/工具目录 | 4 |

## 高风险项

1. 存在 15 个已跟踪文件删除，包含后端服务兼容层、视频 legacy 模块、前端 backup 文件。删除类变更需要逐个确认引用关系和迁移路径。
2. 后端 `app` 改动覆盖 API、模型、pipeline、服务、执行引擎、视频、XMind、AI 生成等多个边界，属于多主题大 diff。
3. 前端 `src` 改动覆盖路由、布局、登录、项目、用例、需求、任务、报告、权限、store、composables、components，影响面很大。
4. `cypress` 全量用例仍有大量未提交修改，和已推送的 Cypress 支持层修复不是同一批提交，需要重新独立验收。
5. 未跟踪中包含迁移脚本、调试脚本、E2E 结果、Playwright 配置、生成类型文件，不能无筛选纳入版本库。
6. `git diff` 输出大量 `CRLF will be replaced by LF` 提示，说明行尾策略仍需谨慎处理，禁止批量格式化全仓。

## 已跟踪删除清单

以下删除需要逐个确认，不建议未经专项 review 直接提交：

```text
app/services/execution_replay_service.py
app/services/task_service.py
app/services/video/legacy_cleanup_mixin.py
app/services/video/legacy_crud_mixin.py
app/services/video/legacy_ffmpeg_mixin.py
app/services/video/legacy_models.py
app/services/video/legacy_service.py
app/services/video/legacy_utils_mixin.py
app/services/video_service.py
src/backup/case_detail.vue
src/backup/case_index.vue
src/backup/dashboard_index.vue
src/backup/requirement_index.vue
src/components/case/FlowNodeCard.vue.backup
src/components/case/FlowSortEditor.vue.backup
```

## 未跟踪文件完整清单

### 数据库迁移

```text
alembic/versions/20260519_add_is_self_test_to_projects.py
alembic/versions/20260519_add_self_test_schedule_to_projects.py
alembic/versions/20260519_add_source_to_bugs.py
alembic/versions/20260522_add_migration_fields.py
alembic/versions/20260527_merge_release_heads.py
```

### 后端新增

```text
app/api/v1/endpoints/case_migration.py
app/api/v1/endpoints/pipeline_dashboard/_utils.py
app/pipelines/scenarios/validation.py
app/schemas/pipeline.py
app/services/lifecycle_service/_capability_rules.py
app/services/lifecycle_service/_capability_service.py
app/services/pipeline_timeout_service.py
app/services/prompt_builder/migration_prompt.py
app/services/prompt_builder/self_test_prompt.py
app/services/scheduler_service.py
app/services/self_test_service.py
app/services/test_case_view/excel_normalize_mixin.py
app/tasks/self_test_scheduler.py
app/utils/browser_controller.py
```

### 前端新增

```text
src/api/auditLog.ts
src/api/auth.ts
src/api/caseMigration.ts
src/api/testCapability.ts
src/api/uiPrototypeFlow.ts
src/api/user.ts
src/auto-imports.d.ts
src/components.d.ts
src/components/case/FlowCompletenessPanel.vue
src/composables/useFlowStats.ts
src/store/generate/__tests__/generateHelpers.spec.ts
src/types/element-plus.ts
src/types/generationCapability.ts
src/views/case/CaseMigration.vue
src/views/system/audit-log/index.vue
src/views/system/audit-log/useAuditLog.ts
src/views/system/test-capability/index.vue
src/views/system/test-capability/useTestCapability.ts
```

### 测试新增

```text
tests/api/test_pipeline_artifact_detail.py
tests/api/test_pipeline_cancel.py
tests/api/test_ui_prototype_flow_summary.py
tests/e2e_t11_t12_routes_permissions.py
tests/pipelines/test_pipeline_progress_ws.py
tests/services/test_action_type_dispatch.py
tests/services/test_bug_fixes_p0_p1.py
tests/services/test_capability_lifecycle_service.py
tests/services/test_pipeline_timeout_service.py
tests/services/test_remote_pull_risk_fixes.py
tests/services/test_scheduler_service.py
tests/services/test_self_test_bug_auto_record.py
tests/services/test_self_test_prompt.py
tests/services/test_self_test_scheduler.py
tests/services/test_self_test_service.py
tests/services/test_spa_navigation.py
tests/services/test_step_retry.py
tests/services/test_structured_assertion.py
tests/services/test_ui_flow_prompt.py
tests/test_case_migration_service.py
```

### E2E / Playwright / Cypress

```text
cypress/e2e/real-main-flow.cy.ts
e2e-playwright/t7-t8-regression.spec.ts
playwright.config.ts
```

### 文档与脚本

```text
docs/ai-case-migration-plan.md
docs/frontend-menu-navigation-implementation-plan.md
docs/next-iteration-backlog.md
scripts/debug_auth.py
scripts/debug_auth2.py
scripts/debug_auth3.py
scripts/debug_auth4.py
scripts/fix_orphan_capability_ids.py
scripts/regression_t9_t10.py
```

### 配置和运行产物

```text
.gitattributes
test-results/.last-run.json
test-results/login-debug.png
```

其中 `test-results/.last-run.json`、`test-results/login-debug.png`、`scripts/debug_auth*.py` 明显更像本地运行或调试产物，默认不应提交。

## 已跟踪修改按主题初分

### 后端 API / 核心 / 模型

覆盖 `app/api/v1/endpoints`、`app/core`、`app/models`、`app/schemas`、`app/crud`。包含认证、项目、pipeline、测试用例、测试任务、UI prototype、websocket、限流、配置、日志、模型字段等变更。

风险：接口行为和数据结构可能变化，需要 API 回归和迁移脚本配套确认。

### 后端服务层

覆盖 `app/services` 下的 audit、captcha、case generation、case quality、execution replay、lifecycle、pipeline、precondition、prompt builder、report、test capability、test case generation、test execution engine、ui spec parser、video、xmind 等模块。

风险：服务层修改跨多个业务域，且同时存在 legacy 删除，需要按业务域拆分提交和验收。

### Pipeline / 迭代 / 生成链路

覆盖 `app/pipelines`、`src/views/iteration`、`src/views/admin/PipelineDashboard.vue`、`src/api/pipeline.ts`、相关测试。

风险：可能影响异步任务、进度、dashboard、artifact 和 websocket 行为，建议单独作为一个主题验收。

### 前端业务页面

覆盖登录、项目、需求、用例、AI 生成、任务、执行、报告、系统角色/用户等页面，以及大量 composables/store/components。

风险：前端改动面非常大，需要 `lint`、`typecheck`、`test:run`、`build`、Cypress 按主题全量复验。

### Cypress / E2E

除已推送的 `cypress/support/e2e.ts` 相关修复外，本地仍有多个 `cypress/e2e/*.cy.ts` 和 `cypress/support/commands.ts` 未提交变更。

风险：这些变更可能改变测试覆盖逻辑，不应和业务代码混在同一提交中。

### 配置和依赖

包含 `.eslintrc.cjs`、`.gitignore`、`package.json`、`pytest.ini`、`requirements.txt`、`vite.config.ts`、`vitest.config.ts`。

风险：配置类改动会影响全仓质量门禁和运行环境，建议独立 review。

### 二进制/图片

```text
hongen_login_result.png
```

风险：属于运行结果图片，需确认是否确实为项目资产；默认不建议提交。

## 建议处理顺序

1. 先保持当前远程基线不动，禁止 `git add .`。
2. 删除或忽略明显本地产物：`test-results/`、`scripts/debug_auth*.py`、临时截图等。
3. 逐主题拆分本地改动：
   - 数据库迁移与模型字段
   - pipeline / scheduler / self-test
   - case migration
   - 前端导航与权限
   - 用例生成与测试点管理
   - 视频 / 执行回放重构
   - Cypress / Playwright 测试改造
4. 每个主题单独执行 `git diff -- <文件列表>`、`git diff --check -- <文件列表>`、相关单元测试和端到端测试。
5. 每个主题单独提交，提交前必须确认 staging 列表，避免把其它主题或运行产物混入。

## 建议复核命令

```powershell
git status --short --branch
git diff --name-status
git ls-files --others --exclude-standard
git diff --check
```

按主题暂存前建议使用：

```powershell
git add -- <明确文件列表>
git diff --cached --name-status
git diff --cached --check
```

## 当前禁止事项

- 不要执行 `git add .`
- 不要批量格式化或重写全仓行尾
- 不要回滚未确认来源的改动
- 不要把 `test-results`、调试脚本、临时截图混入提交
- 不要把 380 个已跟踪改动和 72 个未跟踪文件一次性提交

## 详细实施计划

本计划用于把当前未提交工作区拆分成可审查、可验证、可提交的批次。执行者必须按主题推进，每批只处理同一类变更，不得把无关文件混入同一提交。

### 阶段 0：基线冻结与安全规则

目标：固定当前远程基线，避免后续处理过程中误提交、误回滚或误覆盖用户改动。

执行规则：

- 远程基线固定为 `main@8edb467`，该提交已推送到 `origin/main`。
- 禁止执行 `git add .`、`git add -A`、批量格式化、批量重写行尾、批量回滚。
- 禁止提交或删除未确认来源的用户改动。
- 禁止把 `test-results/`、调试脚本、临时截图、运行日志混入业务提交。
- 每个主题批次必须先执行 `git diff -- <文件列表>`，再执行 `git diff --check -- <文件列表>`。
- 每个主题批次提交前必须执行 `git diff --cached --name-status` 和 `git diff --cached --check`。

基线确认命令：

```powershell
git status --short --branch
git log -1 --oneline
git diff --name-status
git ls-files --others --exclude-standard
```

### 阶段 1：清理/隔离不应入库内容

目标：先识别默认不提交的本地产物和调试文件，避免后续主题拆分时误纳入。

默认不提交：

```text
test-results/
scripts/debug_auth.py
scripts/debug_auth2.py
scripts/debug_auth3.py
scripts/debug_auth4.py
hongen_login_result.png
```

需单独判断：

```text
.gitattributes
.gitignore
工作区未提交改动盘查文档.md
```

处理动作：

- 本阶段不直接删除文件，只做隔离标记。
- 若确认 `test-results/` 和调试脚本无保留价值，再由执行者单独清理。
- `.gitattributes` 只在确认行尾策略后单独提交。
- `.gitignore` 只在确认确实需要新增忽略规则后单独提交。
- 本盘查文档可作为管理文档单独提交，不应和业务代码混提。

确认命令：

```powershell
git status --short -- test-results scripts/debug_auth.py scripts/debug_auth2.py scripts/debug_auth3.py scripts/debug_auth4.py hongen_login_result.png .gitattributes .gitignore "工作区未提交改动盘查文档.md"
git diff -- .gitignore .gitattributes
```

推荐提交信息：

```text
docs: document workspace cleanup plan
chore: update git ignore rules
chore: define repository line ending rules
```

### 阶段 2：按主题拆分提交

#### 2.1 数据库迁移与模型字段

目标：确认数据库结构、模型字段、schema 和 API 响应字段一致。

主要文件范围：

```text
alembic/versions/*.py
app/models/*.py
app/schemas/*.py
app/api/v1/endpoints/*project*.py
app/api/v1/endpoints/*test_case*.py
tests/api/*
tests/schemas/*
```

必须检查：

- Alembic 是否存在多 head，`20260527_merge_release_heads.py` 是否必要。
- 新增字段是否有默认值、nullable 策略和向后兼容处理。
- API schema、ORM model、前端类型是否一致。
- 测试数据或历史数据是否需要 backfill。

推荐验证命令：

```powershell
python -m compileall app scripts
python -m pytest tests/api/test_test_case_crud_api.py tests/api/test_pipeline_precheck_api.py tests/schemas/test_test_case_schema.py -q --tb=short
git diff --check -- alembic app/models app/schemas tests/api tests/schemas
```

推荐提交信息：

```text
feat: add database fields for project and pipeline workflows
```

#### 2.2 Pipeline / Scheduler / Self-test

目标：把 pipeline、调度、自测、超时和 dashboard 相关变更作为一个独立业务链路验证。

主要文件范围：

```text
app/pipelines/
app/services/pipeline_service/
app/services/pipeline_timeout_service.py
app/services/scheduler_service.py
app/services/self_test_service.py
app/tasks/self_test_scheduler.py
app/api/v1/endpoints/pipeline*.py
app/api/v1/endpoints/pipeline_dashboard/
src/api/pipeline.ts
src/views/admin/PipelineDashboard.vue
src/views/admin/usePipelineDashboard.ts
src/views/iteration/
tests/pipelines/
tests/services/test_pipeline_timeout_service.py
tests/services/test_scheduler_service.py
tests/services/test_self_test_service.py
tests/services/test_self_test_scheduler.py
```

必须检查：

- 调度任务是否会在应用启动时重复注册。
- pipeline 状态流转、取消、超时、artifact 详情是否一致。
- websocket 推送和 dashboard 查询是否使用同一状态口径。
- 自测任务失败是否会产生预期 bug 记录，且不会重复写入。

推荐验证命令：

```powershell
python -m compileall app scripts
python -m pytest tests/pipelines tests/services/test_pipeline_timeout_service.py tests/services/test_scheduler_service.py tests/services/test_self_test_service.py tests/services/test_self_test_scheduler.py -q --tb=short
npm run typecheck
npm run test:run
npm run build
git diff --check -- app/pipelines app/services/pipeline_service app/services/pipeline_timeout_service.py app/services/scheduler_service.py app/services/self_test_service.py app/tasks/self_test_scheduler.py src/views/iteration src/views/admin src/api/pipeline.ts tests/pipelines
```

推荐提交信息：

```text
feat: add pipeline scheduling and self-test workflow
```

#### 2.3 Case Migration

目标：独立验证用例迁移功能的后端服务、API、提示词、前端页面和测试。

主要文件范围：

```text
app/api/v1/endpoints/case_migration.py
app/services/case_migration/
app/services/prompt_builder/migration_prompt.py
src/api/caseMigration.ts
src/views/case/CaseMigration.vue
docs/ai-case-migration-plan.md
tests/test_case_migration_service.py
```

必须检查：

- 迁移输入、AI 响应解析、错误降级和日志记录是否一致。
- 前端页面是否只依赖已存在或同批新增的 API。
- 文档描述是否和实际接口一致。

推荐验证命令：

```powershell
python -m compileall app scripts
python -m pytest tests/test_case_migration_service.py -q --tb=short
npm run typecheck
npm run build
git diff --check -- app/api/v1/endpoints/case_migration.py app/services/case_migration app/services/prompt_builder/migration_prompt.py src/api/caseMigration.ts src/views/case/CaseMigration.vue docs/ai-case-migration-plan.md tests/test_case_migration_service.py
```

推荐提交信息：

```text
feat: add test case migration workflow
```

#### 2.4 前端导航、权限、系统管理

目标：把登录、鉴权、权限指令、路由、用户/角色/审计日志/能力管理入口作为一组前端权限链路验证。

主要文件范围：

```text
src/router/
src/directives/permission.ts
src/layouts/
src/views/login/
src/views/system/
src/api/auth.ts
src/api/user.ts
src/api/auditLog.ts
src/api/testCapability.ts
app/api/v1/endpoints/auth_endpoints.py
app/api/v1/endpoints/test_capability.py
app/services/test_capability_service.py
tests/api/test_capability_api.py
tests/services/test_capability_service*.py
```

必须检查：

- 登录态、权限签名、菜单显示和路由守卫是否一致。
- `auth/me` 返回字段是否满足前端权限判断。
- 管理页面是否对普通用户隐藏或拒绝访问。

推荐验证命令：

```powershell
python -m compileall app scripts
python -m pytest tests/api/test_capability_api.py tests/services/test_capability_service.py tests/services/test_capability_service_extended.py -q --tb=short
npm run lint
npm run typecheck
npm run test:run
npm run build
npm run cypress:run
git diff --check -- src/router src/directives src/layouts src/views/login src/views/system src/api/auth.ts src/api/user.ts src/api/auditLog.ts src/api/testCapability.ts app/api/v1/endpoints/auth_endpoints.py app/api/v1/endpoints/test_capability.py app/services/test_capability_service.py
```

推荐提交信息：

```text
feat: add permission-aware navigation and admin views
```

#### 2.5 用例生成、测试点管理、流程编排

目标：把 AI 生成、测试点管理、流程排序、补充表单、用例质量分析和相关前端交互作为一个功能域验证。

主要文件范围：

```text
app/api/v1/endpoints/test_case_ai_generate/
app/api/v1/endpoints/test_case_ai_*.py
app/api/v1/endpoints/test_case_crud*.py
app/api/v1/endpoints/test_point/
app/services/test_case_generation/
app/services/case_generation/
app/services/case_quality/
src/api/case*
src/api/testPoint*
src/components/case/
src/composables/flow*
src/composables/flowSort/
src/store/generate/
src/store/flowSort*
src/views/case/
tests/test_ai_generation*.py
tests/services/test_test_case_generation_validate_mixin.py
tests/services/test_suggestion_mixin.py
tests/api/test_test_case_crud_api.py
```

必须检查：

- AI 生成输入输出结构和前端类型是否一致。
- 流程排序、边类型、撤销恢复、完整性面板是否有测试覆盖。
- 测试点提取和管理页面是否兼容已有数据。
- 不要把 Case Migration、权限管理、pipeline 改动混入本批。

推荐验证命令：

```powershell
python -m compileall app scripts
python -m pytest tests/api/test_test_case_crud_api.py tests/test_ai_generation_full_coverage.py tests/test_ai_generation_branches_supplement.py tests/services/test_test_case_generation_validate_mixin.py tests/services/test_suggestion_mixin.py -q --tb=short
npm run lint
npm run typecheck
npm run test:run
npm run build
npm run cypress:run
git diff --check -- app/api/v1/endpoints/test_case_ai_generate app/api/v1/endpoints/test_case_ai_helpers.py app/api/v1/endpoints/test_case_ai_schemas.py app/api/v1/endpoints/test_case_ai_stream.py app/api/v1/endpoints/test_case_crud.py app/api/v1/endpoints/test_case_crud_batch.py app/api/v1/endpoints/test_point app/services/test_case_generation app/services/case_generation app/services/case_quality src/api/case* src/api/testPoint* src/components/case src/composables/flow* src/store/generate src/store/flowSort* src/views/case tests
```

推荐提交信息：

```text
feat: improve AI case generation and test point workflows
```

#### 2.6 视频与执行回放重构

目标：独立验证视频服务、执行回放、路径工具、旧服务删除和前端回放入口。

主要文件范围：

```text
app/services/video/
app/services/video_service.py
app/services/execution_replay/
app/services/execution_replay_service.py
app/api/v1/endpoints/execution_vis_video_replay.py
src/composables/useVideoReplay.ts
tests/services/test_video*.py
```

必须检查：

- 删除旧服务前必须确认无 import 引用。
- 视频路径生成、清理、CRUD、回放接口是否仍兼容历史数据。
- legacy 文件删除必须和替代实现同批提交，避免中间状态不可运行。

推荐验证命令：

```powershell
rg "execution_replay_service|video_service|legacy_cleanup_mixin|legacy_crud_mixin|legacy_ffmpeg_mixin|legacy_models|legacy_service|legacy_utils_mixin" app tests src
python -m compileall app scripts
python -m pytest tests/services/test_video_service.py tests/services/test_video_extended.py -q --tb=short
npm run typecheck
npm run build
git diff --check -- app/services/video app/services/video_service.py app/services/execution_replay app/services/execution_replay_service.py app/api/v1/endpoints/execution_vis_video_replay.py src/composables/useVideoReplay.ts tests/services/test_video_service.py tests/services/test_video_extended.py
```

推荐提交信息：

```text
refactor: consolidate video replay services
```

#### 2.7 Cypress / Playwright 测试改造

目标：把 E2E 用例改造、Playwright 配置和测试辅助命令作为测试基础设施单独验证。

主要文件范围：

```text
cypress/e2e/
cypress/support/commands.ts
cypress/e2e/real-main-flow.cy.ts
e2e-playwright/
playwright.config.ts
docs/frontend-menu-navigation-implementation-plan.md
```

必须检查：

- 已推送的 `cypress/support/e2e.ts` 不要被无关回退覆盖。
- 新增/修改 E2E 用例不能依赖本地脏数据或手工登录状态。
- Playwright 与 Cypress 的测试产物不能混入提交。

推荐验证命令：

```powershell
npm run lint
npm run typecheck
npm run build
npm run cypress:run
git diff --check -- cypress e2e-playwright playwright.config.ts docs/frontend-menu-navigation-implementation-plan.md
```

如需验证 Playwright：

```powershell
npx playwright test
```

推荐提交信息：

```text
test: expand e2e navigation and workflow coverage
```

#### 2.8 配置与依赖变更

目标：独立审查影响全仓运行方式的配置、依赖和测试配置。

主要文件范围：

```text
.eslintrc.cjs
.gitignore
.gitattributes
package.json
pytest.ini
requirements.txt
vite.config.ts
vitest.config.ts
.trae/rules/project_rules.md
```

必须检查：

- `package.json` 和 lockfile 是否一致；若没有 lockfile 变更，需要确认项目是否不提交 lockfile。
- `requirements.txt` 新增依赖是否真实被代码使用。
- `pytest.ini` 改动是否改变默认测试选择或 warning 策略。
- `.gitattributes` 是否会触发大规模行尾变更。

推荐验证命令：

```powershell
python -m compileall app scripts
npm run lint
npm run typecheck
npm run test:run
npm run build
git diff --check -- .eslintrc.cjs .gitignore .gitattributes package.json pytest.ini requirements.txt vite.config.ts vitest.config.ts .trae/rules/project_rules.md
```

推荐提交信息：

```text
chore: update project tooling configuration
```

### 阶段 3：删除类变更专项处理

目标：确认 15 个删除是否安全，避免删除仍被引用的兼容层或备份文件。

删除清单：

```text
app/services/execution_replay_service.py
app/services/task_service.py
app/services/video/legacy_cleanup_mixin.py
app/services/video/legacy_crud_mixin.py
app/services/video/legacy_ffmpeg_mixin.py
app/services/video/legacy_models.py
app/services/video/legacy_service.py
app/services/video/legacy_utils_mixin.py
app/services/video_service.py
src/backup/case_detail.vue
src/backup/case_index.vue
src/backup/dashboard_index.vue
src/backup/requirement_index.vue
src/components/case/FlowNodeCard.vue.backup
src/components/case/FlowSortEditor.vue.backup
```

必须执行的引用搜索：

```powershell
rg "execution_replay_service|task_service|video_service" app tests
rg "legacy_cleanup_mixin|legacy_crud_mixin|legacy_ffmpeg_mixin|legacy_models|legacy_service|legacy_utils_mixin" app tests
rg "case_detail.vue|case_index.vue|dashboard_index.vue|requirement_index.vue|FlowNodeCard.vue.backup|FlowSortEditor.vue.backup" src docs
```

处理规则：

- 若仍有引用，先迁移引用并补测，再提交删除。
- 若无引用且是明确废弃文件，删除可随对应重构主题提交。
- 前端 backup 删除可单独提交，提交信息使用 `chore: remove obsolete frontend backups`。

### 阶段 4：最终合入门禁

目标：所有主题提交完成后，确认工作区和远程分支达到可推送或可发布状态。

最终验证命令：

```powershell
git status --short --branch
python -m compileall app scripts
python -m pytest tests/test_integration_phase1.py tests/test_case_migration_service.py tests/api/test_test_case_crud_api.py -q --tb=short
npm run lint
npm run typecheck
npm run test:run
npm run build
npm run cypress:run
```

涉及真实浏览器执行链路时追加：

```powershell
python -m pytest tests/test_browser_controller_coverage.py tests/test_browser_controller_real.py tests/test_browser_controller_v2_real.py tests/test_browser_manual.py tests/test_browser_visible.py tests/test_element_locator_coverage.py tests/test_element_locator_real.py tests/test_execution_engine_real.py tests/test_hongen_login_real.py tests/test_precondition_service_real.py -q -o addopts='' -m "real_browser or real_api or not real_browser and not real_api" --tb=short
```

最终验收标准：

- 所有必跑命令通过。
- `git diff --check` 无新增 whitespace error。
- 没有运行产物、调试脚本、临时截图混入提交。
- 每个提交只对应一个主题。
- `git status --short --branch` 中只剩明确暂不处理的本地文件，或工作区完全清洁。

## 执行者检查清单

### Staging 前

```powershell
git status --short --branch
git diff --name-status -- <本批文件列表>
git diff --check -- <本批文件列表>
```

确认事项：

- 文件列表只属于一个主题。
- 没有 `test-results/`、调试脚本、临时截图。
- 没有无关配置或行尾大规模变更。

### Staging 后

```powershell
git diff --cached --name-status
git diff --cached --check
git diff --cached --stat
```

确认事项：

- 暂存区文件数量和主题预期一致。
- 删除文件均已完成引用搜索。
- 未跟踪文件只纳入明确需要的源码、测试或文档。

### Commit 前

```powershell
python -m compileall app scripts
npm run typecheck
```

按主题追加对应 pytest、Vitest、build 或 Cypress 命令。提交信息必须使用单主题描述，不使用笼统描述。

### Push 前

```powershell
git status --short --branch
git log --oneline origin/main..HEAD
```

确认事项：

- 只推送已验收提交。
- 未提交工作区改动不会被误认为已推送。
- 若本地领先多个提交，逐个确认每个提交的主题和验证结果。
